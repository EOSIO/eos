#file(GLOB ABI_FILES "*.abi")
set(ABI_FILES "stltest.abi")
set(INCLUDE_FOLDERS ${CMAKE_SOURCE_DIR}/contracts ${CMAKE_SOURCE_DIR}/contracts/stl ${CMAKE_SOURCE_DIR}/contracts/include)
add_wast_target(stltest "${INCLUDE_FOLDERS}" ${CMAKE_CURRENT_BINARY_DIR})
configure_file("${ABI_FILES}" "${CMAKE_CURRENT_BINARY_DIR}" COPYONLY)
