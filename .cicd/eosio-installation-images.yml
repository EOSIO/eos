steps:

  - label: ":ubuntu: Ubuntu 18.04 - Build"
    command:
      - bash ./.cicd/installation-build.sh"
    env:
      IMAGE_TAG: "ubuntu-18.04"
    agents:
      queue: "automation-eos-builder-fleet"
    timeout: $BUILD_TIMEOUT
    skip: $SKIP_UBUNTU_18