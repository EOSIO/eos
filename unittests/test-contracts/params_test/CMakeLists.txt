set( PARAMS_CONTRACT "params_test" )

if( EOSIO_COMPILE_TEST_CONTRACTS )
   add_contract( ${PARAMS_CONTRACT} ${PARAMS_CONTRACT} ${PARAMS_CONTRACT}.cpp )
else()
   configure_file( ${CMAKE_CURRENT_SOURCE_DIR}/${PARAMS_CONTRACT}.wasm ${CMAKE_CURRENT_BINARY_DIR}/${PARAMS_CONTRACT}.wasm COPYONLY )
   configure_file( ${CMAKE_CURRENT_SOURCE_DIR}/${PARAMS_CONTRACT}.abi  ${CMAKE_CURRENT_BINARY_DIR}/${PARAMS_CONTRACT}.abi  COPYONLY )
endif()
