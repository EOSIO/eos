name: Forked PR
on: [push]

jobs:
  build:
    name: Ubuntu 18.04
    runs-on: ubuntu-latest
    steps:
      - name: Build
        uses: actions/checkout@v2
        run: ./.cicd/build.sh
        env:
          IMAGE_TAG: ubuntu-18.04-pinned
          PLATFORM_TYPE: pinned
