steps:
  - command: |
        echo "New pipeline!" && \
        pwd && \
        ls && \
        cd Docker/builder && \
        ls -lah /usr/bin && \
        gcloud
    label: "Docker build"
    agents:
      role: "automation-builder-fleet"
      aws:instance-type: "m5d.2xlarge"
    timeout: 300

  - wait
